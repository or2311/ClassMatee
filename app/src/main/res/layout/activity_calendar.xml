<?xml version="1.0" encoding="utf-8"?>
<!-- 
    זהו קובץ העיצוב (Layout) של מסך לוח המבחנים והאירועים (Calendar).
    הוא מאפשר למשתמש לבחור תאריך ולראות את כל המבחנים והאירועים שנקבעו לאותו יום.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F9FA"
    android:fitsSystemWindows="true">

    <!-- סרגל כלים עליון עם כותרת המסך -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:title="לוח מבחנים ואירועים"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- גלילה המכילה את לוח השנה ואת רשימת האירועים -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- כרטיס המכיל את לוח השנה (CalendarView) -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="20dp"
                app:cardCornerRadius="16dp"
                app:cardElevation="4dp">

                <CalendarView
                    android:id="@+id/full_calendar_view"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:theme="@style/Theme.ClassMate" />

            </com.google.android.material.card.MaterialCardView>

            <!-- כותרת לרשימת האירועים של היום הנבחר -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="12dp"
                android:layout_marginStart="4dp"
                android:text="אירועים בתאריך שנבחר"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#333333" />

            <!-- רשימה המציגה את המבחנים/אירועים שנמצאו בתאריך הנבחר -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/exams_list_recycler_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:nestedScrollingEnabled="false"
                tools:listitem="@layout/item_exam" />

            <!-- הודעה שמופיעה כאשר אין אירועים בתאריך הנבחר -->
            <TextView
                android:id="@+id/no_events_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:gravity="center"
                android:text="אין אירועים בתאריך זה"
                android:textColor="#999999"
                android:textSize="16sp"
                android:visibility="gone" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <!-- כפתור צף להוספת אירוע חדש (זמין בעיקר למנהלים) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_event"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:contentDescription="הוספת אירוע חדש"
        app:srcCompat="@android:drawable/ic_input_add"
        app:tint="@color/white"
        app:backgroundTint="?attr/colorPrimary" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
